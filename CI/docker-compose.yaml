version: '3.9'

services:
  playwright:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playwright-tests
    working_dir: /app
    volumes:
      - ../:/app
    tty: true
